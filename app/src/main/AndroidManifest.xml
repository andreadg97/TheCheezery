<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="diaz.andrea.thecheezery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProductosActivity"></activity>
        <activity android:name=".PersonalizedActivity" />
        <activity android:name=".CombosActivity" />
        <activity android:name=".SaltiesActivity" />
        <activity android:name=".SweetsActivity" />
        <activity android:name=".HotDrinksActivity" />
        <activity android:name=".ColdDrinksActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>